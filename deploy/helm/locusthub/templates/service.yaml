apiVersion: v1
kind: Service
metadata:
  name: locusthub-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: locusthub-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
